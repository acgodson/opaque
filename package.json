{
  "name": "opaque-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "web",
    "elizaos-agent"
  ],
  "scripts": {
    "build:plugin": "bun run --cwd packages/plugin-opaque build",
    "build:enclave": "bun run --cwd packages/enclave build",
    "build:agent": "bun run --cwd packages/agent build",
    "build:web": "bun run --cwd web build",
    "build": "bun run build:agent && bun run build:plugin && bun run build:enclave && bun run build:web",
    "dev:agent": "bun run --cwd elizaos-agent dev",
    "dev:web": "bun run --cwd web dev",
    "clean": "rm -rf packages/*/dist packages/*/node_modules elizaos-agent/node_modules web/.next web/node_modules node_modules"
  }
}