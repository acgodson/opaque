{
  "name": "0xvisor-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @0xvisor/agent build && pnpm --filter @0xvisor/web build",
    "dev": "pnpm --filter @0xvisor/web dev",
    "build:agent": "pnpm --filter @0xvisor/agent build",
    "build:web": "pnpm --filter @0xvisor/web build"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
