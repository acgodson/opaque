{
  "name": "opaque-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @opaque/agent build && pnpm --filter @opaque/web build",
    "dev": "pnpm --filter @opaque/web dev",
    "build:agent": "pnpm --filter @opaque/agent build",
    "build:web": "pnpm --filter @opaque/web build"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "js-sdsl": "^4.4.2"
  }
}
